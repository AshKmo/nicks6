# This is an implementation of the factorial and Y combinator functions #

(\let

let (\f (\g g g) (\g f(\x g g x)) ) \rec

let (\c \a \b [b, a].(c)) \if

let (rec \fac \x
	(
		if (x = 0)
		(\0 1)
		(\0 x * fac(x - 1))
	) ()
) \fac


fac 6


) \x \f f x
